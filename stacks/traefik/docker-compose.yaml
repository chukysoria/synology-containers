---
services:
  traefik:
    image: ghcr.io/chukysoria/traefik:v1.1.45@sha256:7a9846d0945063f9693dcf41d5fec4d96c372c361073b819113e949a3df35f44
    networks:
      frontend:
      macvlan:
        ipv4_address: 192.168.1.49
      firefly-frontend-net:
    dns:
      - 1.1.1.1
      - 8.8.8.8
    extra_hosts:
      - host.docker.internal:192.168.1.48
    environment:
      # General LinuxServer.io
      PUID: ${PUID:-1029}
      PGID: ${PGID:-65536}
      TZ: ${TZ:-Europe/Madrid}
      # Traefik
      DUCKDNS_TOKEN: ${DUCKDNS_TOKEN:?DuckDNS API Token}
    volumes:
      - ${BASE_PATH:-/volume2/docker/}certs:/certs:rw
      - ${BASE_PATH:-/volume2/docker/}traefik:/config:rw
      - /var/run/docker.sock:/var/run/docker.sock
    labels:
      traefik.enable: true
      # Routers
      traefik.http.routers.traefik-rtr.rule: Host(`traefik.${DOMAIN:-boulebar.duckdns.org}`)
      traefik.http.routers.traefik-rtr.service: api@internal
      # Middlewares
      traefik.http.routers.traefik-rtr.middlewares: authentik@docker
    restart: unless-stopped

  sablier:
    image: sablierapp/sablier:1.11.1@sha256:000fab8e34adec9587b2424cc91e8526403ebea4b0636e9667f4123d450fe165
    command:
      - start
      - --provider.name=docker
      - --provider.docker.strategy=pause
    volumes:
      - '/var/run/docker.sock:/var/run/docker.sock'
    networks:
      - frontend

networks:
  frontend:
    external: true # Defined in portainer
  macvlan:
    external: true # Defined in base
  firefly-frontend-net:
    external: true # Defined in base
